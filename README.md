# py_10w_2501046

## 25
- 표준 입력으로 키와 값을 받아 딕셔너리 생성
- 예시 입력:
  keys = alpha bravo charlie delta echo foxtrot golf
  values = 30 40 50 60 70 80 90
- `zip()` 함수로 키-값 매핑

## 26
- `park` 딕셔너리 선언 후 english와 science 점수 출력

## 27
- `kim` 딕셔너리의 모든 과목 점수를 100점으로 수정
- `update()` 사용

## 28
- `lee` 딕셔너리에서 `'english'` 키가 존재하면 삭제
- `pop()` 사용

## 29
- `lim` 딕셔너리의 모든 key-value 출력
- `items()` 사용

## 30
- `choi` 딕셔너리에서 90점 이상인 과목의 key만 출력
- dictionary comprehension 사용

## 31
- `yoo` 딕셔너리의 과목 평균 점수 출력
- `sum()`과 `len()` 사용
